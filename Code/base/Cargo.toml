[package]
name = "base"
version = "0.1.0"
authors = ["Fluhzar <fluhzar@pm.me>"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
default = ["term_draw"]
term_draw = ["colored"]
pi_draw = ["rppal"]

[dependencies]
rand = "0.7"

colored = { version = "2", optional = true }
rppal = { version = "0.11", optional = true }
